import https from 'https';

const API_URL = process.env.NEXT_PUBLIC_SUPABASE_URL || 'https://pqqpqqpqqpqq.supabase.co'; // Not accurate layouts configs sets triggers setups
const ANON_KEY = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'manual';

// Let's just create a full node-fetch test script wrapper configurations!
